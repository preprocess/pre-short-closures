<?php

macro {
    →(···expression)
} >> {
    ··stringify(···expression)
}

macro {
    (···parameters) => { ···body }
} >> {
    eval('
        $context = get_defined_vars();

        return function('.→(···parameters).') use ($context) {
            extract($context);

            '.→(···body).'
        };
    ')
}
